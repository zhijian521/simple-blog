# 部署与上线验证清单

> 面向 `yuwb.dev` 的静态部署。当前线上是 **nginx 自建**（实测），HSTS 与 80→443 跳转已生效，
> 缺的是缓存策略、其余安全头、`rss.xml` 的 Content-Type，以及 `www` 域名的解析。
> 本文里的配置可直接复制，改完用第 4 节的命令逐条验证。

## 1. 上线前的一次性准备

| 项 | 操作 | 现状（实测） |
|---|---|---|
| DNS：裸域 | `yuwb.dev` A 记录指向服务器 | 已有 |
| DNS：www | 加 `www` 的 A（或 CNAME 到裸域）解析 | **缺失**：`https://www.yuwb.dev/` 直接连不上（不是跳转） |
| 证书 | 覆盖 `yuwb.dev` **和** `www.yuwb.dev` 两个名字 | 需确认；只签裸域时 www 会证书告警 |
| nginx 版本 | `nginx -v`，确认 `mime.types` 是否已含 `woff2` | 未验证 |

## 2. nginx 配置

放在 `http {}` 里（缓存策略用 `map`，**不要**写进 `location`）：

```nginx
map $uri $cache_control {
    default                        "public, max-age=0, must-revalidate";  # HTML 与目录 URL
    ~^/_astro/                     "public, max-age=31536000, immutable"; # 文件名带内容 hash
    ~\.(webp|png|ico|jpe?g|svg)$   "public, max-age=604800";              # 图片名不带 hash
    ~^/(rss\.xml|sitemap.*\.xml|robots\.txt|manifest\.json)$ "public, max-age=3600";
}

server {
    listen 443 ssl http2;
    server_name yuwb.dev;
    root /var/www/yuwb.dev;          # app/dist 的内容
    index index.html;
    charset utf-8;

    ssl_certificate     /etc/nginx/ssl/yuwb.dev.crt;
    ssl_certificate_key /etc/nginx/ssl/yuwb.dev.key;

    # 保持 404 状态码：不要用 try_files 兜底到 404 页，否则所有不存在的 URL 都变成软 404
    error_page 404 /404.html;

    # 已有的 HSTS 保持不变；includeSubDomains / preload 先不要加（撤回很慢）
    add_header Strict-Transport-Security "max-age=31536000" always;
    add_header X-Content-Type-Options    "nosniff" always;
    add_header X-Frame-Options           "DENY" always;
    add_header Referrer-Policy           "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy        "camera=(), microphone=(), geolocation=(), payment=(), usb=()" always;
    add_header Content-Security-Policy   "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self'; font-src 'self'; media-src 'self'; manifest-src 'self'; connect-src 'none'; object-src 'none'; base-uri 'self'; form-action 'none'; frame-src 'none'; frame-ancestors 'none'; upgrade-insecure-requests" always;
    add_header Cache-Control             $cache_control always;
    # 注意：location 里再写 add_header 会清掉上面这一整组，所以下面几个 location 只改类型、不加头

    location = /rss.xml       { default_type application/rss+xml; charset utf-8; }
    location = /manifest.json { default_type application/manifest+json; }
    location ~* \.woff2$      { default_type font/woff2; }
    location ~ /\.            { deny all; }   # dist 里有 /images/.gitkeep、/videos/.gitkeep

    location / { try_files $uri $uri/index.html =404; }   # 顺带把 /blog/ 的 403 变成 404
}

# www 统一 301 到裸域（DNS 解析生效后才有意义）
server {
    listen 443 ssl http2;
    server_name www.yuwb.dev;
    ssl_certificate     /etc/nginx/ssl/yuwb.dev.crt;
    ssl_certificate_key /etc/nginx/ssl/yuwb.dev.key;
    return 301 https://yuwb.dev$request_uri;
}

# 80 → 443（现已生效，保留即可）
server {
    listen 80;
    server_name yuwb.dev www.yuwb.dev;
    return 301 https://$host$request_uri;
}
```

**CSP 为什么这么写**（依据是构建产物实测）：

- 外部 `<script src>` **0 个**、可执行内联脚本 **0 个**（8 个 `<script>` 全是 `application/ld+json` 数据块），所以 `script-src 'self'` 就够，**不需要** `'unsafe-inline'`。
- 但 `style-src` **必须**带 `'unsafe-inline'`：全站有 **614 个**内联 `style="--shiki-light:…"`（Shiki 的代码高亮输出）。写成 `style-src 'self'` 会让代码块颜色全部失效（静默降级，只有控制台报错）。
- `frame-ancestors` 只能走响应头，写进 `<meta>` 会被忽略。

**gzip / brotli**：确认 `http {}` 里有 `gzip on;`（text/html 一般默认已开）。若 nginx 编了 brotli 模块，可再开 brotli——实测 CSS 能再省 33%、HTML 再省 19%。

**换托管平台（Vercel / Netlify / Cloudflare Pages）**：改用 `app/public/_headers` 承载同样的头，并**先修掉构建脚本对 devDependency 的顶层 import**（已在代码里改成按需动态加载，见 `app/scripts/build-font-subset.mjs`），否则 `npm ci --omit=dev` 环境下 `prebuild` 会直接失败。

## 3. 部署流程

```bash
# 本地（app/ 目录）
npm run build          # prebuild 会自动同步图片并检查字体子集覆盖
# 产物在 app/dist/，整目录上传覆盖服务器上的旧版本
rsync -av --delete app/dist/ user@server:/var/www/yuwb.dev/
# 服务器
nginx -t && systemctl reload nginx
```

要点：

- **只传 `dist/` 的内容**，不要传 `docs/`、`app/src/`、`node_modules/`。
- `--delete` 很重要：删掉的文章不会从服务器上自动消失。
- `app/dist/` 里的文件名带内容 hash（`_astro/Base.<hash>.css`），所以新版本不会和旧缓存冲突。
- 域名换环境时用 `SITE_URL` 环境变量覆盖，不必改代码。

## 4. 上线后验证清单

逐条跑，括号里是预期结果。

```bash
# 基础可达性与跳转
curl -sI https://yuwb.dev/                     # 200；Cache-Control: public, max-age=0, must-revalidate
curl -sI http://yuwb.dev/                      # 301 → https
curl -sI https://www.yuwb.dev/                 # 301 → https://yuwb.dev/（配置前是连不上）

# 缓存策略
curl -sI https://yuwb.dev/_astro/Base.<hash>.css   # Cache-Control: public, max-age=31536000, immutable
curl -sI https://yuwb.dev/images/cover-44-og.png   # Cache-Control: public, max-age=604800

# 安全头
curl -sI https://yuwb.dev/ | grep -iE 'strict-transport|x-content-type|x-frame|referrer-policy|permissions-policy|content-security-policy'

# 内容类型
curl -sI https://yuwb.dev/rss.xml              # Content-Type: application/rss+xml; charset=utf-8
curl -sI https://yuwb.dev/manifest.json        # Content-Type: application/manifest+json
curl -sI https://yuwb.dev/_astro/lxgw-wenkai-site.<hash>.woff2   # font/woff2

# 状态码
curl -s -o /dev/null -w '%{http_code}\n' https://yuwb.dev/missing-page-xyz/   # 404（不是 200）
curl -s -o /dev/null -w '%{http_code}\n' https://yuwb.dev/blog/               # 404（配置前是 403）
curl -s -o /dev/null -w '%{http_code}\n' https://yuwb.dev/404.html           # 200（文件本身）

# 内容新鲜度：确认线上已是最新版本
curl -s https://yuwb.dev/ | grep -c 'intro__bio'      # 1（旧版本是 0）
curl -s -o /dev/null -w '%{http_code}\n' https://yuwb.dev/page/1/            # 200（旧版本是 404）

# 字体子集：应当只有 2 个 woff2，且 WenKai 约 250KB
curl -s https://yuwb.dev/blog/seo-guide/ | grep -o '_astro/[^"]*woff2' | sort -u

# 结构化数据与分享卡片
#   https://search.google.com/test/rich-results?url=https://yuwb.dev/blog/seo-guide/
#   https://cards-dev.twitter.com/validator（或直接发给微信/飞书看卡片）
```

浏览器端再确认一次（上线后清一次缓存）：

1. DevTools → Console：**不应有 CSP 违规报错**（若报 `style-src`，说明内联样式被拦，检查 CSP 是否漏了 `'unsafe-inline'`）。
2. DevTools → Network：筛选 JS，**应该是 0 个**（本站零客户端 JS）；字体只有 2 个 woff2。
3. 手机宽度 320px：文章页**不应出现整页横向滚动**（表格内部横滑是预期行为）。
4. 打印预览一篇长文，确认阅读体验可接受（本站未做打印样式，属已知取舍）。

## 5. 当前线上现状（本次实测，2026-09-26）

| 项 | 状态 |
|---|---|
| `https://yuwb.dev/` | 200，但内容**落后于本地代码**：首页 5562 B / 无简介区 / 无归档链接 |
| `/page/1/` | 404（线上还没有归档页） |
| `/blog/` | 403（目录无 index） |
| `www.yuwb.dev` | 连不上 |
| `Strict-Transport-Security` | 已生效（max-age=31536000） |
| `Cache-Control` | 无 |
| 其余安全头 | 无 |
| `/rss.xml` | `Content-Type: text/xml` |
| 软 404 | 不存在（`error_page` 正确） |
| `Server` | `nginx`（未暴露版本号） |

**结论：本地 `main` 已经比线上新，改完必须重新构建并上传，否则所有改动都不会生效。**

## 6. 回滚

- 每次上传前把线上目录改名备份（`mv /var/www/yuwb.dev /var/www/yuwb.dev.bak.$(date +%s)`），出问题改回来即可。
- nginx 配置改动前 `cp` 一份，`nginx -t` 失败就还原并 `reload`。
- 本仓库的每个批次都是独立提交，代码回滚用 `git revert <commit>`。
