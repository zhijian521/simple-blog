---
import Base from '../../layouts/Base.astro';
import { formatDate, getNotes } from '../../lib/notes.js';

export function getStaticPaths() {
    return getNotes().map((note) => ({ params: { slug: note.slug }, props: { note } }));
}

const { note } = Astro.props;
const siteUrl = (import.meta.env.SITE_URL || 'http://localhost:4321').replace(/\/$/, '');
const description = note.description || `${note.title}，语无边的技术笔记。`;
const schema = {
    '@context': 'https://schema.org',
    '@type': 'BlogPosting',
    headline: note.title,
    description,
    datePublished: note.date,
    mainEntityOfPage: `${siteUrl}${note.url}`,
    author: { '@type': 'Person', name: '语无边' },
};
---
<Base title={note.title} description={description} path={note.url} schema={schema}>
    <article class="article">
        <header class="article__header">
            <a class="article__back" href="/">返回首页</a>
            <h1 class="article__title">{note.title}</h1>
            <time class="article__date" datetime={note.date}>{formatDate(note.date)}</time>
            {note.description && <p class="article__description">{note.description}</p>}
        </header>
        <div class="prose" set:html={note.html} />
    </article>
</Base>
