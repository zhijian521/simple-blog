{
    "name": "simple-blog",
    "private": true,
    "type": "module",
    "engines": {
        "node": ">=22.12.0"
    },
    "scripts": {
        "predev": "node scripts/sync-media.js && node scripts/build-font-subset.mjs --check",
        "dev": "astro dev",
        "prebuild": "node scripts/sync-media.js && node scripts/build-font-subset.mjs --check",
        "build": "astro build",
        "fonts": "node scripts/build-font-subset.mjs",
        "preview": "astro preview"
    },
    "dependencies": {
        "@astrojs/sitemap": "^3.6.0",
        "astro": "^7.3.5",
        "gray-matter": "^4.0.3",
        "markdown-it": "^14.1.0",
        "shiki": "^4.4.3"
    },
    "devDependencies": {
        "subset-font": "^2.9.0"
    }
}
